# REQUIRED for Electron Forge compatibility
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true

# Keep dependencies local to each app. Public hoist moves deps to the workspace root
# node_modules, which Electron Forge does not traverse for monorepo app packaging.
